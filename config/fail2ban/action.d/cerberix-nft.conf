# Cerberix fail2ban action — block via nftables AI blocklist set
[Definition]
actionban   = nft add element inet cerberix_ai blocklist { <ip> timeout <bantime>s }
actionunban = nft delete element inet cerberix_ai blocklist { <ip> }
